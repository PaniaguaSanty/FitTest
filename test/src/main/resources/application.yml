spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gym_db
    username: root
    password: santyroot
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

  output:
    ansi:
      enabled: always # permite ver el log como colores

logging:
  level:
    root: info # permite ver los logs de consola


gym:
  openapi:
    url: "http://localhost:8080"
    description: "API for managing gym operations, clients, routines, and nutrition plans."
    contact:
      name: "Santiago Agustín Paniagua"
      email: "paniaguasanty10@gmail.com"
      url: "https://github.com/PaniaguaSanty"
    title: "Gym Management API"
    version: "1.0"